# 密钥与本地环境（sk- key 只活在 server/.env，绝不入库）
server/.env
.env

node_modules/
*.log
Thumbs.db
.DS_Store

# IDE / 智能体配置（非项目代码，不入公开仓库）
.cursor/

# 冗余备份
*.bak

# 临时验证脚本（跑完即删，不入仓库）
.tmp-*
tests/.tmp-*

# 内部文档：比赛策略与竞品分析，与工程无关，不入公开仓库。
# 其中的技术设计部分已脱敏拆分为 doc/architecture.md（已入库）。
doc/开发文档.md

# 覆盖率与 E2E 产物
coverage/
.nyc_output/
test-results/
playwright-report/

# 运行时持久化数据（数据源 / 分享页 / 知识库 / 用量计数）
# 这是用户数据，不该进仓库；容器部署时挂卷到这里。
data/
.tmp-data-*/

# KC-Chat 运行时产物（插件自动生成，请勿入库）
.cursor/mcp.json
.cursor/kc-mcp/
.cursor/kc-mcp-messages/
.cursor/kc-chat-exports/
